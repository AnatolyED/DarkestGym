public enum PlayerNumber
{
    None,
    First,
    Second
}

public enum Action
{
    Idle,
    Move,
    Attack,
    Block,
    Ability,
    EndMove
}

public enum SkillType
{
    Active,
    Passive
}